
plugins {
    id 'java-gradle-plugin'
    id 'groovy'
    id "maven-publish"
}

group = "com.temp"
version = "0.0.1"

gradlePlugin {
    plugins {
        qux {
            id = 'qux'
            implementationClass = 'qux.QuxPlugin'
        }
    }
}

publishing {
  repositories {
    mavenLocal()
  }
}

